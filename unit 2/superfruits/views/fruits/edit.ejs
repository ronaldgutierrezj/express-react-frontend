<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/header.ejs") %>

    <main>
      <div>
        <form action="/fruits/<%= fruit._id %>?_method=PUT" method="post">
          <fieldset>
            <legend>Edit <%= fruit.name %></legend>
            <label
              >Name:
              <input type="text" name="name" placeholder="enter fruit name" 
              value="<%= fruit.name %>"/>
            </label>
            <label>
              COLOR:
              <input type="text" name="color" placeholder="enter fruit color" value="<%= fruit.color %>"/>
            </label>
            <label>
              READY TO EAT:
              <input type="checkbox" 
              name="readyToEat" 
              <%= fruit.readyToEat ? "checked" : "" %>
              />
            </label>
            <input type="submit" value="UPDATE FRUIT" />
          </fieldset>
        </form>
      </div>
    </main>
  </body>
</html>